# приоритеты ТЯ
steps=( "${steps[@]}" "transport-platform-api-web.*" )
steps=( "${steps[@]}" "platform-commons-war.*" )
steps=( "${steps[@]}" "custodian-distr-impl-ear.*" )
steps=( "${steps[@]}" "custodian-selfcheck.*" )
steps=( "${steps[@]}" "logger-platform.*" )
steps=( "${steps[@]}" "access_platform_client_ear.*" )
steps=( "${steps[@]}" "audit2-client-platform.*" )
steps=( "${steps[@]}" "audit2-wildfly-agent.*" )
steps=( "${steps[@]}" "com-sbt-ott-api-client-war.*" )
steps=( "${steps[@]}" "transport-md-wf-web.* transport-md-ear.*" )
steps=( "${steps[@]}" "seap-lib-deps.*" )
steps=( "${steps[@]}" "azimuth-api.*" )
steps=( "${steps[@]}" "configurator-core.*" )
steps=( "${steps[@]}" "ignite-provider.*" )
steps=( "${steps[@]}" "launcher-module.*" )
steps=( "${steps[@]}" "dpl-lite-ear.*" )
steps=( "${steps[@]}" "cci-mmt-impl-ear.*" )
steps=( "${steps[@]}" "cci-mmt-impl-war.*" )
steps=( "${steps[@]}" "cc-routing-war.*" )
steps=( "${steps[@]}" "cs-mmt-client-ear.*" )
steps=( "${steps[@]}" "cs-snowflake-ear.*" )
steps=( "${steps[@]}" "cs-oracle-ear.* cs-mmt-server.*" )
steps=( "${steps[@]}" "ott-service-client-cfg-listener.*" )
steps=( "${steps[@]}" "rps-lib.*" )
steps=( "${steps[@]}" "s4-client-pprb-jbm.*" )

# приоритеты OTT
steps=( "${steps[@]}" "ojdbc8.*" )
steps=( "${steps[@]}" "ejbca.*" )
steps=( "${steps[@]}" "com-sbt-authz-core-engine.*" )
steps=( "${steps[@]}" "ott-service.*" )

# приоритеты БТС Архивирование_v4
steps=( "${steps[@]}" "pprbod-source-provider-v4.*" )
steps=( "${steps[@]}" "arch-journal-cdm-init.*" )
steps=( "${steps[@]}" "arch-journal-gbk-init.*" )
steps=( "${steps[@]}" "arch-journal-ucp-consents-init.*" )
steps=( "${steps[@]}" "pprbod-handler.*" )
steps=( "${steps[@]}" "pprbod-applied-journal-handler.*" )
steps=( "${steps[@]}" "pprbod-offline-dq-processor-v4.*" )
steps=( "${steps[@]}" "pprbod-offline-dq-collector-v4.*" )
steps=( "${steps[@]}" "pprbod-stream-processor.*" )

# приоритеты БТС Архивирование
steps=( "${steps[@]}" "arch-journal-acquiring-1p.*" )
steps=( "${steps[@]}" "arch-journal-gbk.*" )
steps=( "${steps[@]}" "arch-journal-rspit.*" )
steps=( "${steps[@]}" "arch-journal-ucp.*" )
steps=( "${steps[@]}" "arch-journal-ucp-rb.*" )
steps=( "${steps[@]}" "arch-journal-ucp-kb.*" )
steps=( "${steps[@]}" "data-driver-cards-impl.*" )
steps=( "${steps[@]}" "data-driver-p2p-impl.*" )
steps=( "${steps[@]}" "arch-journal-acc-engine.*" )
steps=( "${steps[@]}" "arch-journal-bps-calc.*" )
steps=( "${steps[@]}" "arch-journal-cdm.*" )
steps=( "${steps[@]}" "arch-journal-opf-ind.*" )
steps=( "${steps[@]}" "pprbod-source-provider.*" )
steps=( "${steps[@]}" "pprbod-source-provider-rb.*" )
steps=( "${steps[@]}" "pprbod-source-provider-kb.*" )
steps=( "${steps[@]}" "pprbod-offline-dq-processor.*" )
steps=( "${steps[@]}" "pprbod-offline-dq-collector.*" )

# приоритеты ЕПК
steps=( "${steps[@]}" "ucp-dictionaries-service.*" )
steps=( "${steps[@]}" "ucp-core.*" )
steps=( "${steps[@]}" "ucp-2mr-core.*" )
steps=( "${steps[@]}" "ucp-2mr-rule.*" )
steps=( "${steps[@]}" "ucp-bgtasks-external.*" )

# приоритеты ПСИ/ПРОМ
steps=( "${steps[@]}" "oip-core.*" )
steps=( "${steps[@]}" "oip-core-oip-p2p.*" )
steps=( "${steps[@]}" "sip-p2p-router.*" )
steps=( "${steps[@]}" "sip-p2p-demand.*" )
steps=( "${steps[@]}" "nsi-facade.*" )
steps=( "${steps[@]}" "cdm-module-oracle.*" )

# приоритеты КБТ СИПов
steps=( "${steps[@]}" "hdpmonitoring-agent.*" )
steps=( "${steps[@]}" "scripts-autpay.*" )
steps=( "${steps[@]}" "scripts-cashdesk-operations.*" )
steps=( "${steps[@]}" "scripts-cashdesk-transactions.*" )
steps=( "${steps[@]}" "scripts-cashmng.*" )
steps=( "${steps[@]}" "scripts-cep-ucprb-dm.*" )
steps=( "${steps[@]}" "scripts-cep-ucprb-mdm.*" )
steps=( "${steps[@]}" "scripts-cep-ucprb-ps.*" )
steps=( "${steps[@]}" "scripts-cep-ucprb-tcrm.*" )
steps=( "${steps[@]}" "scripts-cep-ucprb-w.*" )
steps=( "${steps[@]}" "scripts-fatca.*" )
steps=( "${steps[@]}" "scripts-fcca.*" )
steps=( "${steps[@]}" "scripts-gfl.*" )
steps=( "${steps[@]}" "scripts-loan-person.*" )
steps=( "${steps[@]}" "scripts-rating-corp.*" )
steps=( "${steps[@]}" "scripts-rco.*" )
steps=( "${steps[@]}" "scripts-rdm.*" )
steps=( "${steps[@]}" "scripts-spd.*" )
steps=( "${steps[@]}" "scripts-statements-ear.*")
steps=( "${steps[@]}" "scripts-ucp-cep.*" )
steps=( "${steps[@]}" "scripts-ucp-corp.*" )
steps=( "${steps[@]}" "scripts-ucp-corp-elgo.*" )
steps=( "${steps[@]}" "scripts-ucp-corp-ri.*" )
steps=( "${steps[@]}" "scripts-ucprb-cep-dm.*" )
steps=( "${steps[@]}" "scripts-ucprb-cep-tcrm.*" )
steps=( "${steps[@]}" "scripts-ucprb-cep-uas.*" )
steps=( "${steps[@]}" "scripts-uss-orc.*" )
steps=( "${steps[@]}" "wizard-cashoper.*" )
steps=( "${steps[@]}" "wizard-cpc.*" )
steps=( "${steps[@]}" "wizard-eks-payment-orders.*" )
steps=( "${steps[@]}" "wizard-online-mgr.*" )
steps=( "${steps[@]}" "wizard-rating-person.*" ) 

# приоритеты для БПС (Крамарев Сергей Николаевич)
steps=( "${steps[@]}" "namedcounter-module.*" )
steps=( "${steps[@]}" "calendar-module.*" )
steps=( "${steps[@]}" "mega-bas.*" )
steps=( "${steps[@]}" "cdm-module.*" )
steps=( "${steps[@]}" "pprbac-register.*" )
steps=( "${steps[@]}" "cdm-request.*" )
steps=( "${steps[@]}" "multizone-service-module.*" )
steps=( "${steps[@]}" "client-relocation-module.*" )

steps=( "${steps[@]}" "bgp-agent.*" )

# приоритеты СПР
steps=( "${steps[@]}" "bps-calculations.*" )
steps=( "${steps[@]}" "spr-data-collection.*" )
steps=( "${steps[@]}" "spr-modelcalculate.*" )
steps=( "${steps[@]}" "spr-strategy-executor.*" )
steps=( "${steps[@]}" "spr-business-rules-service.*" )
steps=( "${steps[@]}" "spr-kernel.*" )

# приоритеты P2P
steps=( "${steps[@]}" "oip-core-sip-p2p-classic.*" )
steps=( "${steps[@]}" "sip-p2p-classic.*" )

# приоритеты ИМ
steps=( "${steps[@]}" "integrator.*" )

# приоритеты РКО
steps=( "${steps[@]}" "legal-payment.*" )

# приоритеты ZALOGI
steps=( "${steps[@]}" "pledges-lib-web.*" )
